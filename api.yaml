enabled: true
route: /api
resources:
    page:
        enabled: true
        fields: []
    pages:
        enabled: true
        # Only return these fields for each page
        fields: ['title', 'url', 'visible', 'modified']
    user:
        enabled: true
        fields: []
    users:
        enabled: true
        fields: ['username', 'fullname', 'title']
    plugin:
        enabled: true
        fields: []
    plugins:
        enabled: true
        fields: []
    config:
        enabled: true
        fields: []
    configs:
        enabled: true
        # Prevents API from accessing these config files
        ignore_files: ['streams']
