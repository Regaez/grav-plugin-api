enabled: true
route: /api
resources:
    page:
        enabled: true
        fields: []
    pages:
        enabled: true
        fields: ['title', 'url', 'visible', 'modified']
    user:
        enabled: true
        fields: []
    users:
        enabled: true
        fields: ['username', 'fullname', 'title']
